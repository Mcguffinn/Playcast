services:
  - type: web
    name: weather-playlist-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn playcast:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
    healthCheckPath: /
    autoDeploy: true